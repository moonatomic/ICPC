#include <bits/stdc++.h>

int main() {
    int ans;
    int add;
    int n, k;
    int key;
    std::vector<int> keys;

    for (int i = 0; i < n; i++) {
        std::cin >> key;
        keys[i] = key;

        
        if (i < n%k) {
            continue;
        }
    }
}